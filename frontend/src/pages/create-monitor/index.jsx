import BackButton from "components/BackButton";
import React from "react";

const CreateMonitor = () => {
  return (
    <main>
      <BackButton />
    </main>
  );
};

export default CreateMonitor;
